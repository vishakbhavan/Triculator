<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rps.triculator">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo256"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo256"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Area" android:windowSoftInputMode="stateHidden" ></activity>
        <activity android:name=".Side" android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".Angle" android:windowSoftInputMode="stateHidden" />
        <activity android:name=".About"></activity>
    </application>

</manifest>